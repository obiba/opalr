% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datashield.quota.R
\name{dsadmin.quota_save}
\alias{dsadmin.quota_save}
\title{Save a DataSHIELD usage quota}
\usage{
dsadmin.quota_save(
  opal,
  limit,
  period = "WEEKLY",
  metric = "EXECUTION_TIME",
  type = "system",
  subject = NULL,
  enabled = TRUE
)
}
\arguments{
\item{opal}{Opal object.}

\item{limit}{The allowance, in minutes of whatever the metric names. Zero is a meaningful value:
it forbids DataSHIELD altogether for the subject.}

\item{period}{The rolling window the usage is summed over: 'WEEKLY' (default) or 'DAILY'. There
is no reset instant, capacity returns as old activity ages out of the window.}

\item{metric}{The usage metric: 'EXECUTION_TIME' (default) is the R server's cpu time consumed
(an idle session costs nothing), 'SESSION_TIME' is the time R sessions are held, whether they
compute or not.}

\item{type}{The type of subject: system (default), user or group.}

\item{subject}{The user name or the group name, not applicable when the type is system.}

\item{enabled}{Whether the quota applies (default is TRUE).}
}
\value{
The saved DataSHIELD quota, invisibly.
}
\description{
Create or update the DataSHIELD usage quota of a subject: the system default, a group or a user.
A subject can have one quota per usage metric, therefore saving replaces the quota that the
subject already had for this metric, if any.
}
\details{
When a user runs DataSHIELD, the quota that applies to them is their own if they have one, else
the most permissive of the ones given to the groups they belong to, else the system default. A
disabled quota is ignored by this resolution.

Requires DataSHIELD or system administration permissions.
}
\examples{
\dontrun{
o <- opal.login('administrator','password', url='https://opal-demo.obiba.org')
# 10 hours of R execution time per week for everyone
dsadmin.quota_save(o, limit = 600)
# 2 hours of R execution time per day for a user
dsadmin.quota_save(o, limit = 120, period = 'DAILY', type = 'user', subject = 'andrei')
# 8 hours of R session time per week for a group
dsadmin.quota_save(o, limit = 480, metric = 'SESSION_TIME', type = 'group', subject = 'datashield')
dsadmin.quotas(o)
opal.logout(o)
}
}
\seealso{
Other DataSHIELD quotas:
\code{\link[=dsadmin.quota]{dsadmin.quota()}},
\code{\link[=dsadmin.quota_delete]{dsadmin.quota_delete()}},
\code{\link[=dsadmin.quota_enable]{dsadmin.quota_enable()}},
\code{\link[=dsadmin.quota_exists]{dsadmin.quota_exists()}},
\code{\link[=dsadmin.quota_usage]{dsadmin.quota_usage()}},
\code{\link[=dsadmin.quotas]{dsadmin.quotas()}},
\code{\link[=opal.quotas]{opal.quotas()}}
}
\concept{DataSHIELD quotas}
