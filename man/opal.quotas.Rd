% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opal.quota.R
\name{opal.quotas}
\alias{opal.quotas}
\title{Get own R usage quotas}
\usage{
opal.quotas(opal, context = "DataSHIELD", df = TRUE)
}
\arguments{
\item{opal}{Opal object.}

\item{context}{The R execution context: 'DataSHIELD' (default), 'R', 'Import', 'Export', 'SQL'
or 'Analyse'.}

\item{df}{Return a data.frame (default is TRUE)}
}
\value{
The R quota usages as a data.frame (columns are: context, user, metric, period, limit,
used, exceeded, windowStartDate, nextCreditDate, openSessionsCount) or a list. Limits and usages
are expressed in minutes, and are NA when no quota applies to the metric.
}
\description{
Get, for each usage metric, the R usage quota that applies to the current user in an execution
context and what they have consumed against it. Every metric is reported, whether or not a quota
applies to it: an entry with no limit is the unlimited case, and nothing is measured then.
}
\details{
Reporting on oneself requires no special permission. See \link{dsadmin.quota_usage} for the
quotas of another user in the DataSHIELD context.
}
\examples{
\dontrun{
o <- opal.login('administrator','password', url='https://opal-demo.obiba.org')
opal.quotas(o)
opal.quotas(o, context = 'R')
opal.logout(o)
}
}
\seealso{
Other DataSHIELD quotas:
\code{\link[=dsadmin.quota]{dsadmin.quota()}},
\code{\link[=dsadmin.quota_delete]{dsadmin.quota_delete()}},
\code{\link[=dsadmin.quota_enable]{dsadmin.quota_enable()}},
\code{\link[=dsadmin.quota_exists]{dsadmin.quota_exists()}},
\code{\link[=dsadmin.quota_save]{dsadmin.quota_save()}},
\code{\link[=dsadmin.quota_usage]{dsadmin.quota_usage()}},
\code{\link[=dsadmin.quotas]{dsadmin.quotas()}}
}
\concept{DataSHIELD quotas}
